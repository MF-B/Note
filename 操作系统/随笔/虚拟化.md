## cpu虚拟化
1. 用户模式很好理解，那内核模式和特权模式的区别呢？
	1. 其实是同一种模式。内核模式可以进行特权操作
2. 如何理解陷阱？

## 内存虚拟化
1. 进程保存的状态信息是什么？
2. 栈来保存函数信息？是指pc等寄存器的值吗？
3. 堆是什么数据结构。堆就是树，但是父子节点之间有大小顺序
4. 为什么动态分配的和用户管理的内存要用堆管理？
5. 虚拟化追求透明/高效/安全
6. 基址寄存器居然是用来虚拟内存地址定位的
7. 简单的静态重定位是用软件修改指令中的地址实现的。但是访问保护需要软硬件配合实现。而且静态重定位一旦完成就不再修改
8. 动态重定向发生在运行时，主要是靠基址寄存器。那界限寄存器起什么作用呢？
9. 界限寄存器有两种工作方式，但是逻辑等价
10. 操作系统需要记录内存的使用情况，用什么数据结构呢？
### 分段
1. 什么是分段?
	1. 段：segment，想想汇编语言的segment
2. 为了分段，还需要标记识别是那个段，(为了支持栈段)是否反向增长。为了共享内存，可以增加保护位
3. 分段减少了内存浪费。代价是？
### 空闲内存管理
1. 解决外部碎片问题---由malloc()或系统分段虚拟内存导致的
2. 由malloc库管理的空间叫堆，不清楚是否和数据结构堆有关。
数值表示
非数值表示
	字符表示
	汉字表示
		机内码
			GB2312编码(国标码)=区位码＋A0A0H
				后拓展为GBK编码
			Unicode码
		字形码
			点阵显示
		矢量字体
数据校验
	方法
		校验码(原始数据+校验位)解决方案
			流程
				 原始数据->校验码---(不可靠)传输--->校验码'->解码数据
			原理
				扩大码距(海明距离)来识别错误代码
			分类
				奇/偶校验
					简单奇偶校验
						编码规则
							增加校验位,使其最终校验码中1的个数为奇数/偶数
					多重奇偶校验
						交叉奇偶校验
						SEC码校验(是一种海明校验码)
							格式
								n(校验码位数)=k(原始信息位数)+r(检验位位数)
							编码规则
								基本原理
									原数据分成若干偶校验组，每组设置一位偶校验位。每位都要位于2个以上的检验组。
								分组规则
									1. 校验位放入海明码的幂次方位上，其余放数据位。
									2 .每个校验组对应检错码的一个位,以此可得每个校验组应该检测海明码的哪些位数								
							检错码
								共r位，可表示$2^r-1$ 种1位错
								所以应该设置$n=k+r<=2^r -1$
						SECDED码(拓展海明码)
				循环冗余校验
		
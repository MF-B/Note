## 实现
1. 先分段，(单周期)通常分成取值->译码->执行->访存->写回阶段
2. 分段不难，但是解决流水线冲突略显复杂
## 冲突
1. 因为有**指令相关**(数据/结构/控制相关)，才导致流水线冲突的出现
2. 不同的指令相关采用不同的解决方法
### 结构冲突
1. 因为多条指令在同一时钟使用同一个部件导致的冲突
2. 然而哈佛结构(独立指令存储器和数据存储器)不会存在此问题，现代流水线可采用此方案
3. 也可以通过阻塞程序计数器PC来解决，也就是往指令流水线里塞一个空操作。会导致性能损失
### 控制冲突
1. 